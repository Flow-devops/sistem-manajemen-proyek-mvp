# 1. METADATA PROYEK
name: nama_proyek_anda 
description: Sebuah sistem manajemen proyek flow MVP.
publish_to: 'none' # Mencegah publikasi otomatis

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Versi Dart SDK yang didukung

# 2. DEPENDENSI (PACKAGE/LIBRARY)
dependencies:
  flutter:
    sdk: flutter
  
  provider: ^6.1.2
  flutter_dotenv: ^5.1.0

  # DEPENDENSI UNTUK PROYEK FLOW ANDA
  supabase_flutter: ^2.0.0 
  firebase_core: ^2.25.3
  firebase_messaging: ^14.7.14

# 3. KONFIGURASI KHUSUS FLUTTER
flutter:
  uses-material-design: true
  
  # fonts:
  # assets:

  assets:
  - .env
  - assets/images/logo.png
